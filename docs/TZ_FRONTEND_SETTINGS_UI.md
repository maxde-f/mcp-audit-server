# ТЗ: UI для управления настройками оценки

## 1. Общее описание

### 1.1 Цель
Создать веб-интерфейс для просмотра и настройки параметров оценки стоимости разработки в audit-platform.

### 1.2 Технологии
- **Framework**: Next.js 14+ (App Router)
- **UI**: Tailwind CSS + shadcn/ui
- **State**: React Query (TanStack Query)
- **Forms**: React Hook Form + Zod
- **Charts**: Recharts
- **Icons**: Lucide React

### 1.3 Backend API
- **Base URL**: `http://localhost:8080`
- **Всего endpoints**: 55
- **Ключевые группы**:
  - `/api/settings/*` - управление настройками
  - `/api/workspace/*` - рабочие пространства
  - `/api/integrations/*` - GitHub, Google Drive, документы

---

## 2. Структура страниц

```
/settings
├── /rates          # Региональные ставки
├── /estimation     # Параметры оценки (COCOMO, AI)
├── /methodologies  # Методологии оценки
├── /validation     # Настройки валидации
└── /audit-log      # История изменений

/workspace
├── /               # Статус workspace
├── /analyses       # Список анализов
├── /reports        # Отчеты
└── /calibration    # Калибровка

/integrations
├── /github         # GitHub репозитории
├── /gdrive         # Google Drive
└── /documents      # Загруженные документы
```

---

## 3. Страница: Региональные ставки (/settings/rates)

### 3.1 Описание
Просмотр и редактирование почасовых ставок для 8 регионов.

### 3.2 API Endpoints
```
GET    /api/settings/rates                 # Получить все ставки
GET    /api/settings/rates/{region_id}     # Получить ставку региона
PUT    /api/settings/rates/{region_id}     # Обновить ставку
POST   /api/settings/rates/{region_id}     # Добавить регион
```

### 3.3 UI Компоненты

#### 3.3.1 Таблица регионов
```tsx
interface RegionalRatesTable {
  columns: [
    { key: 'region', label: 'Регион', sortable: true },
    { key: 'currency', label: 'Валюта' },
    { key: 'junior', label: 'Junior', editable: true },
    { key: 'middle', label: 'Middle', editable: true },
    { key: 'senior', label: 'Senior', editable: true },
    { key: 'typical', label: 'Typical', editable: true },
    { key: 'overhead', label: 'Overhead %', editable: true },
    { key: 'actions', label: '' }
  ];
}
```

#### 3.3.2 Форма редактирования региона
```tsx
interface RateEditForm {
  fields: {
    name: string;           // "Ukraine"
    currency: 'USD' | 'EUR' | 'GBP';
    symbol: string;         // "$"
    rates: {
      junior: number;       // min: 5, max: 300
      middle: number;       // >= junior
      senior: number;       // >= middle
      typical: number;      // min: 5, max: 300
    };
    overhead: number;       // min: 1.0, max: 2.0
  };
  validation: {
    // Показывать ошибки от ValidationBounds
    showBoundsWarning: boolean;
  };
}
```

#### 3.3.3 Визуализация
- **Bar Chart**: Сравнение ставок по регионам
- **Heatmap**: Junior/Middle/Senior по регионам

### 3.4 Валидация (Anti-Hallucination)
```
GET /api/settings/validation-bounds
```
Показывать предупреждения при:
- Ставка < $5 или > $300
- Middle < Junior или Senior < Middle
- Overhead < 1.0 или > 2.0

### 3.5 Wireframe
```
┌─────────────────────────────────────────────────────────┐
│ Региональные ставки                        [+ Добавить] │
├─────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────┐ │
│ │ 🔍 Поиск региона...                                 │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ┌────────┬───────┬────────┬────────┬────────┬────────┐ │
│ │ Регион │ Валюта│ Junior │ Middle │ Senior │ Actions│ │
│ ├────────┼───────┼────────┼────────┼────────┼────────┤ │
│ │ 🇺🇦 UA  │ USD   │ $15    │ $25    │ $45    │ ✏️ 🗑️  │ │
│ │ 🇵🇱 PL  │ EUR   │ €25    │ €40    │ €60    │ ✏️ 🗑️  │ │
│ │ 🇩🇪 DE  │ EUR   │ €45    │ €70    │ €100   │ ✏️ 🗑️  │ │
│ │ 🇺🇸 US  │ USD   │ $50    │ $85    │ $130   │ ✏️ 🗑️  │ │
│ └────────┴───────┴────────┴────────┴────────┴────────┘ │
│                                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │               📊 Сравнение ставок                   │ │
│ │   UA  ████████░░░░░░░░░░░░░  $25                    │ │
│ │   PL  ████████████░░░░░░░░░  €40                    │ │
│ │   DE  ████████████████████░  €70                    │ │
│ │   US  ██████████████████████ $85                    │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 4. Страница: Параметры оценки (/settings/estimation)

### 4.1 API Endpoints
```
GET    /api/settings/params                    # Все параметры
PUT    /api/settings/params/cocomo             # Обновить COCOMO
PUT    /api/settings/params/ai-productivity    # Обновить AI rates
```

### 4.2 Секции

#### 4.2.1 COCOMO II Parameters
```tsx
interface CocomoParams {
  a: number;            // 0.1 - 5.0, default: 0.5
  b: number;            // 0.5 - 1.5, default: 0.85
  c: number;            // 1.0 - 4.0, default: 2.0
  d: number;            // 0.2 - 0.5, default: 0.35
  hours_per_pm: number; // 120 - 200, default: 160
}
```

UI элементы:
- Слайдеры с числовым input
- Формула в реальном времени: `Effort = {a} × (KLOC)^{b} × EAF`
- Калькулятор: ввести LOC → показать результат

#### 4.2.2 AI Productivity Rates
```tsx
interface AIProductivity {
  pure_human: number;   // 1 - 100, default: 25 hrs/KLOC
  ai_assisted: number;  // 1 - 50,  default: 8 hrs/KLOC
  hybrid: number;       // 1 - 40,  default: 6.5 hrs/KLOC
}
```

UI элементы:
- Слайдеры
- Сравнительная диаграмма: Human vs AI vs Hybrid

#### 4.2.3 Effort Multipliers
```tsx
interface EffortMultipliers {
  team_experience: { low: number; nominal: number; high: number };
  process_maturity: { low: number; nominal: number; high: number };
  tool_support: { low: number; nominal: number; high: number };
  requirements_volatility: { low: number; nominal: number; high: number };
}
```

### 4.3 Wireframe
```
┌─────────────────────────────────────────────────────────┐
│ Параметры оценки                           [Сбросить]   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ▼ COCOMO II Modern                                      │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Коэффициент a:  0.5  ├────●────────────────┤ 5.0   │ │
│ │ Экспонента b:   0.85 ├──────●──────────────┤ 1.5   │ │
│ │ Schedule c:     2.0  ├────────●────────────┤ 4.0   │ │
│ │ Schedule d:     0.35 ├────●────────────────┤ 0.5   │ │
│ │ Hours/PM:       160  ├────────●────────────┤ 200   │ │
│ │                                                     │ │
│ │ Формула: Effort = 0.5 × (KLOC)^0.85 × EAF          │ │
│ │                                                     │ │
│ │ Калькулятор:                                        │ │
│ │ LOC: [  15000  ]  → 810 часов (~5 человеко-месяцев)│ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ▼ AI Productivity (hrs/KLOC)                            │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Pure Human:    25  ├──────────────●────────┤ 100   │ │
│ │ AI-Assisted:   8   ├────●──────────────────┤ 50    │ │
│ │ Hybrid:        6.5 ├──●────────────────────┤ 40    │ │
│ │                                                     │ │
│ │ ┌───────────────────────────────────────────────┐  │ │
│ │ │ Human    ██████████████████████████  25 hrs   │  │ │
│ │ │ AI       ████████░░░░░░░░░░░░░░░░░░  8 hrs    │  │ │
│ │ │ Hybrid   ██████░░░░░░░░░░░░░░░░░░░░  6.5 hrs  │  │ │
│ │ └───────────────────────────────────────────────┘  │ │
│ │ Экономия с Hybrid: 74% времени                     │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│                              [Сохранить] [Отмена]       │
└─────────────────────────────────────────────────────────┘
```

---

## 5. Страница: Методологии (/settings/methodologies)

### 5.1 API Endpoints
```
GET    /api/settings/methodologies                    # Список
PUT    /api/settings/methodologies/{id}/toggle        # Вкл/выкл
```

### 5.2 Список методологий
```tsx
interface Methodology {
  id: string;           // "cocomo", "gartner", etc.
  name: string;         // "COCOMO II (Modern)"
  formula: string;      // "Effort = 0.5 × (KLOC)^0.85"
  source: string;       // "Boehm et al. (2000)"
  confidence: string;   // "High", "Medium"
  enabled: boolean;     // Toggle
  min_loc?: number;     // Минимум LOC для применения
}
```

### 5.3 UI
- Карточки методологий с toggle
- Подсветка активных
- Tooltip с формулой и источником

### 5.4 Wireframe
```
┌─────────────────────────────────────────────────────────┐
│ Методологии оценки                     [Включить все]   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ┌───────────────────┐ ┌───────────────────┐            │
│ │ ✓ COCOMO II      │ │ ✓ Gartner         │            │
│ │ High confidence   │ │ High confidence   │            │
│ │ E = 0.5×KLOC^0.85│ │ D = words/650×C   │            │
│ │ [Toggle: ON]      │ │ [Toggle: ON]      │            │
│ └───────────────────┘ └───────────────────┘            │
│                                                         │
│ ┌───────────────────┐ ┌───────────────────┐            │
│ │ ✓ IEEE 1063      │ │ ○ SEI SLIM        │            │
│ │ High confidence   │ │ Medium confidence │            │
│ │ D = pages/1.5×C  │ │ Min 10K LOC       │            │
│ │ [Toggle: ON]      │ │ [Toggle: OFF]     │            │
│ └───────────────────┘ └───────────────────┘            │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 6. Страница: Валидация (/settings/validation)

### 6.1 API Endpoints
```
GET    /api/settings/validation-bounds          # Границы
POST   /api/settings/validate/estimate          # Валидация оценки
POST   /api/settings/validate/pert              # Валидация PERT
```

### 6.2 UI элементы

#### 6.2.1 Таблица границ валидации
```tsx
interface ValidationBounds {
  rates: { min: 5, max: 300 };
  cocomo_a: { min: 0.1, max: 5.0 };
  cocomo_b: { min: 0.5, max: 1.5 };
  hours_per_pm: { min: 120, max: 200 };
  ai_productivity: { min: 1, max: 100 };
  overhead: { min: 1.0, max: 2.0 };
  estimate_hrs_per_kloc: { min: 2, max: 200 };
  pert_ratio_max: 10;
}
```

#### 6.2.2 Тестер валидации
- Ввести hours, LOC, cost
- Показать результат валидации
- Цветовая индикация: зеленый/желтый/красный

### 6.3 Wireframe
```
┌─────────────────────────────────────────────────────────┐
│ Настройки валидации (Anti-Hallucination)                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ Границы валидации                                       │
│ ┌────────────────────┬──────────┬──────────┐           │
│ │ Параметр           │ Min      │ Max      │           │
│ ├────────────────────┼──────────┼──────────┤           │
│ │ Ставка ($/час)     │ 5        │ 300      │           │
│ │ COCOMO a           │ 0.1      │ 5.0      │           │
│ │ COCOMO b           │ 0.5      │ 1.5      │           │
│ │ Hrs/KLOC           │ 2        │ 200      │           │
│ │ PERT ratio         │ -        │ 10x      │           │
│ └────────────────────┴──────────┴──────────┘           │
│                                                         │
│ Тестер валидации                                        │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Hours: [ 1000   ]  LOC: [ 50000  ]  Cost: [ 35000 ] │ │
│ │                                                     │ │
│ │ [Проверить]                                         │ │
│ │                                                     │ │
│ │ Результат: ✅ Valid                                 │ │
│ │ Confidence: High                                    │ │
│ │ Hrs/KLOC: 20 (норма: 2-200)                        │ │
│ │ Warnings: нет                                       │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 7. Страница: История изменений (/settings/audit-log)

### 7.1 API Endpoints
```
GET /api/settings/audit-log?limit=100
```

### 7.2 Таблица логов
```tsx
interface AuditLogEntry {
  timestamp: string;
  action: string;        // "update_rate", "toggle_methodology"
  section: string;       // "regional_rates.ua", "estimation_params"
  user: string;          // "api", "admin"
  old_value: any;
  new_value: any;
  validation_passed: boolean;
  validation_message: string;
}
```

### 7.3 UI
- Таблица с фильтрацией по action/section
- Diff view для old_value vs new_value
- Цветовая индикация: passed (зеленый), failed (красный)

---

## 8. Страница: Workspace (/workspace)

### 8.1 API Endpoints
```
POST   /api/workspace/init                    # Инициализация
GET    /api/workspace/status?path=...         # Статус
GET    /api/workspace/analyses?path=...       # Список анализов
POST   /api/workspace/report?path=...         # Генерация отчета
POST   /api/workspace/calibration?path=...    # Калибровка
```

### 8.2 UI элементы

#### 8.2.1 Статус workspace
- Путь к workspace
- Кол-во анализов
- Последние анализы
- Статус калибровки

#### 8.2.2 Список анализов
- Таблица: ID, repo_path, stage, health, debt, validation
- Действия: просмотр, удаление
- Фильтры: по дате, по статусу валидации

#### 8.2.3 Генерация отчетов
- Выбор типа: summary, review, compliance, cost_analysis
- Предпросмотр markdown
- Скачивание

### 8.3 Wireframe
```
┌─────────────────────────────────────────────────────────┐
│ Workspace: /Users/dev/audit-workspace                   │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐│
│ │ 12        │ │ 8/12      │ │ 10/15     │ │ 5 samples ││
│ │ Analyses  │ │ Avg Health│ │ Avg Debt  │ │ Calibrated││
│ └───────────┘ └───────────┘ └───────────┘ └───────────┘│
│                                                         │
│ Последние анализы                                       │
│ ┌──────────┬──────────────────┬───────┬────────┬──────┐│
│ │ ID       │ Repository       │ Stage │ Health │ Valid││
│ ├──────────┼──────────────────┼───────┼────────┼──────┤│
│ │ abc123   │ github.com/...   │ MVP   │ 8/12   │ ✅   ││
│ │ def456   │ gitlab.com/...   │ Beta  │ 10/12  │ ✅   ││
│ │ ghi789   │ local/project    │ Alpha │ 6/12   │ ⚠️   ││
│ └──────────┴──────────────────┴───────┴────────┴──────┘│
│                                                         │
│ Генерация отчета                                        │
│ ┌─────────────────────────────────────────────────────┐ │
│ │ Тип: [Summary ▼]  Analysis: [abc123 ▼]              │ │
│ │                                                     │ │
│ │ [Сгенерировать]                                     │ │
│ └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 9. Страница: Интеграции (/integrations)

### 9.1 GitHub
```
GET  /api/integrations/github/repo?url=...
GET  /api/integrations/github/branches?url=...
GET  /api/integrations/github/repos
```

UI:
- Ввод URL репозитория
- Показ информации: name, description, branches, size
- Кнопка "Запустить аудит"

### 9.2 Google Drive
```
GET   /api/integrations/gdrive/list
POST  /api/integrations/gdrive/download/{file_id}
```

UI:
- File picker
- Список загруженных документов
- Загрузка в Documents

### 9.3 Documents
```
POST   /api/integrations/documents/upload
GET    /api/integrations/documents
POST   /api/integrations/documents/{id}/parse
DELETE /api/integrations/documents/{id}
```

UI:
- Drag & drop upload
- Список документов с parsed/not parsed статусом
- Просмотр извлеченных требований

---

## 10. Компоненты

### 10.1 Общие компоненты
```tsx
// Таблица с сортировкой и фильтрами
<DataTable columns={...} data={...} />

// Редактируемая ячейка
<EditableCell value={...} onSave={...} validation={...} />

// Слайдер с input
<SliderWithInput min={...} max={...} step={...} />

// Validation badge
<ValidationBadge status="valid" | "warning" | "error" />

// Diff viewer
<DiffViewer oldValue={...} newValue={...} />

// Статистическая карточка
<StatCard title="..." value={...} icon={...} />
```

### 10.2 Формы
```tsx
// Zod схемы для валидации
const rateSchema = z.object({
  junior: z.number().min(5).max(300),
  middle: z.number().min(5).max(300),
  senior: z.number().min(5).max(300),
  typical: z.number().min(5).max(300),
}).refine(data => data.middle >= data.junior, {
  message: "Middle must be >= Junior"
});
```

### 10.3 Графики
```tsx
// Bar chart для сравнения ставок
<RatesComparisonChart regions={...} />

// Pie chart для распределения методологий
<MethodologiesChart methodologies={...} />

// Line chart для истории калибровки
<CalibrationHistoryChart samples={...} />
```

---

## 11. State Management

### 11.1 React Query
```tsx
// Получение ставок
const { data: rates } = useQuery({
  queryKey: ['rates'],
  queryFn: () => fetch('/api/settings/rates').then(r => r.json())
});

// Обновление ставки
const mutation = useMutation({
  mutationFn: (data) => fetch(`/api/settings/rates/${regionId}`, {
    method: 'PUT',
    body: JSON.stringify(data)
  }),
  onSuccess: () => queryClient.invalidateQueries(['rates'])
});
```

### 11.2 Оптимистичные обновления
```tsx
const updateRate = useMutation({
  mutationFn: updateRateAPI,
  onMutate: async (newRate) => {
    await queryClient.cancelQueries(['rates']);
    const previous = queryClient.getQueryData(['rates']);
    queryClient.setQueryData(['rates'], old => ({
      ...old,
      regions: { ...old.regions, [regionId]: newRate }
    }));
    return { previous };
  },
  onError: (err, _, context) => {
    queryClient.setQueryData(['rates'], context.previous);
    toast.error('Ошибка сохранения');
  }
});
```

---

## 12. Требования к UX

### 12.1 Обратная связь
- Toast уведомления при успехе/ошибке
- Inline ошибки в формах
- Loading states для всех операций

### 12.2 Валидация
- Real-time валидация в формах
- Показывать границы из ValidationBounds
- Предупреждения при приближении к границам

### 12.3 Responsive
- Desktop: полная функциональность
- Tablet: адаптированные таблицы
- Mobile: упрощенный вид (карточки вместо таблиц)

### 12.4 Доступность
- Keyboard navigation
- ARIA labels
- High contrast mode support

---

## 13. Безопасность

### 13.1 Валидация на фронте
- Zod схемы для всех форм
- Санитизация input

### 13.2 API запросы
- CORS настроен на бэкенде
- Retry logic для failed requests
- Error boundaries

---

## 14. Приоритеты реализации

### Phase 1: Core (MVP)
1. ✅ Страница ставок (CRUD)
2. ✅ Страница параметров оценки
3. ✅ Валидация с ValidationBounds

### Phase 2: Extended
4. Методологии (toggle)
5. Audit log (view)
6. Workspace status

### Phase 3: Integrations
7. GitHub интеграция
8. Document upload
9. Report generation

### Phase 4: Polish
10. Charts и визуализация
11. Calibration UI
12. Google Drive

---

## 15. API Response Examples

### GET /api/settings/rates
```json
{
  "regions": {
    "ua": {
      "name": "Ukraine",
      "currency": "USD",
      "symbol": "$",
      "rates": {"junior": 15, "middle": 25, "senior": 45, "typical": 28},
      "overhead": 1.20
    },
    "de": {
      "name": "Germany",
      "currency": "EUR",
      "symbol": "€",
      "rates": {"junior": 45, "middle": 70, "senior": 100, "typical": 72},
      "overhead": 1.40
    }
  },
  "updated_at": "2024-12-18T10:00:00Z",
  "updated_by": "api"
}
```

### POST /api/settings/validate/estimate
```json
// Request
{"hours": 1000, "loc": 50000, "cost": 35000, "methodology": "COCOMO II"}

// Response
{
  "valid": true,
  "warnings": [],
  "errors": [],
  "confidence": "high"
}
```

### GET /api/settings/validation-bounds
```json
{
  "rates": {"min": 5, "max": 300},
  "cocomo": {
    "a": {"min": 0.1, "max": 5.0},
    "b": {"min": 0.5, "max": 1.5}
  },
  "hours_per_pm": {"min": 120, "max": 200},
  "ai_productivity": {"min": 1, "max": 100},
  "overhead": {"min": 1.0, "max": 2.0},
  "pert_ratio_max": 10,
  "estimate_hrs_per_kloc": {"min": 2, "max": 200}
}
```

---

## 16. Зависимости (package.json)

```json
{
  "dependencies": {
    "next": "^14.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "@tanstack/react-query": "^5.0.0",
    "react-hook-form": "^7.48.0",
    "zod": "^3.22.0",
    "@hookform/resolvers": "^3.3.0",
    "tailwindcss": "^3.3.0",
    "recharts": "^2.10.0",
    "lucide-react": "^0.294.0",
    "@radix-ui/react-dialog": "^1.0.0",
    "@radix-ui/react-dropdown-menu": "^2.0.0",
    "@radix-ui/react-slider": "^1.1.0",
    "@radix-ui/react-switch": "^1.0.0",
    "@radix-ui/react-tabs": "^1.0.0",
    "@radix-ui/react-toast": "^1.1.0",
    "class-variance-authority": "^0.7.0",
    "clsx": "^2.0.0",
    "tailwind-merge": "^2.0.0"
  }
}
```

---

## 17. Структура файлов

```
ui/
├── src/
│   ├── app/
│   │   ├── settings/
│   │   │   ├── rates/
│   │   │   │   └── page.tsx
│   │   │   ├── estimation/
│   │   │   │   └── page.tsx
│   │   │   ├── methodologies/
│   │   │   │   └── page.tsx
│   │   │   ├── validation/
│   │   │   │   └── page.tsx
│   │   │   └── audit-log/
│   │   │       └── page.tsx
│   │   ├── workspace/
│   │   │   ├── page.tsx
│   │   │   ├── analyses/
│   │   │   │   └── page.tsx
│   │   │   └── calibration/
│   │   │       └── page.tsx
│   │   ├── integrations/
│   │   │   ├── github/
│   │   │   │   └── page.tsx
│   │   │   └── documents/
│   │   │       └── page.tsx
│   │   ├── layout.tsx
│   │   └── page.tsx
│   ├── components/
│   │   ├── ui/              # shadcn components
│   │   ├── settings/
│   │   │   ├── RatesTable.tsx
│   │   │   ├── RateEditForm.tsx
│   │   │   ├── CocomoSliders.tsx
│   │   │   └── MethodologyCard.tsx
│   │   ├── workspace/
│   │   │   ├── AnalysesList.tsx
│   │   │   └── ReportGenerator.tsx
│   │   └── charts/
│   │       ├── RatesComparisonChart.tsx
│   │       └── CalibrationChart.tsx
│   ├── lib/
│   │   ├── api.ts           # API client
│   │   ├── queries.ts       # React Query hooks
│   │   └── validation.ts    # Zod schemas
│   └── types/
│       └── settings.ts      # TypeScript types
├── tailwind.config.ts
└── package.json
```
