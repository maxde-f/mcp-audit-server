# Core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0
pyyaml>=6.0

# HTTP client
httpx>=0.25.0

# MCP HTTP Server (SSE)
starlette>=0.32.0

# Database
psycopg2-binary>=2.9.0  # PostgreSQL
redis>=5.0.0            # Redis cache

# Optional quality tools
# radon          # Python complexity
# jscpd          # Code duplication (install via npm)
# ruff           # Python linter
# bandit         # Python security
# semgrep        # Multi-language security
